create table tbpessoa(
	pescodigo integer,
	pesnome varchar (100) not null,
	pessexo integer not null,
	pestipo integer not null,
	logcodigo integer,
	CONSTRAINT pk_tbpessoa PRIMARY KEY (pescodigo),
	--FK PARA AS TABELAS DE SEXO E TIPO
);

-- tbpessoasexo
create table tbpessoasexo(
	pessexo integer,	
	descricaosexo varchar (100) not null,
	CONSTRAINT pk_tbpessoasexo 
		PRIMARY KEY (pessexo)
);

create table tbpessoatipo(
	pestipo integer,	
	descricaotipo varchar (100) not null,
	CONSTRAINT pk_tbpessoatipo 
		PRIMARY KEY (pestipo)
);

-- busca 
select 
	 case 
		when pestipo = 1 then 'Física'
		when pestipo = 2 then 'Jurídica'
	 end tipopessoa,
	 case 
		when pessexo = 1 then 'Feminino'
		when pestipo = 2 then 'Masculino'
		else 'Não informado'
	 end sexo,
	 TBPESSOA.* 
from TBPESSOA 


