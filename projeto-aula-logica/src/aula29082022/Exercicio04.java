package aula29082022;

public class Exercicio04 {
    // Exercicio 11.24 da Apostila de Exercicios JAVA.
    //O algoritmo de César de criptografia de strings é uma versão melhorada do algoritmo rot13
    //(veja exercício 11.23): o seu funcionamento é o mesmo, só que, em vez de substituir cada caracter por um caracter treze posições depois, o algoritmo de César recebe um valor chamado
    //chave, e usa esse valor como o número de posições que devem ser puladas para a criptografia.
    //Por exemplo, se a chave for 1, o algoritmo pulará uma posição ao codificar as letras, então
    //se a string passada for “Java”, o resultado será “Kbwb”. O algoritmo de decodificação
    //deve receber a mesma chave, só que deve substituir os caracteres da string por valores em
    //posições anteriores.
    //Escreva um método estático codificaCésar na classe Criptografia que implemente
    //o algoritmo de César, recebendo como argumentos uma string e uma chave (valor numérico)
    //e retornando a string criptografada. Esse método deve considerar que somente as letras
    //não-acentuadas devem ser criptografadas, as letras acentuadas, números, espaços e outros
    //símbolos devem continuar como estão. Escreva também o método decodificaCésar.
    //Dica: Para simplificar o algoritmo, considere que o valor da chave só pode estar entre 1 e 25.
    //Existem ao menos duas maneiras de implementar esse algoritmo.

}
